# 处理意见箱（标准件指令）

> 用户说「处理意见箱」时按此流程执行。这是应用自我迭代的固定动作。

## 流程

0. **占班**：写 `data/ai-shift.json`（holder=你的名字、task=处理意见箱、started_at=现在）。有人在岗且 2 小时内有更新 → 先等，不抢
1. **读箱**：读 `data/feedback/` 下所有 `"status": "new"` 的意见；空箱则释放排班表并告知
2. **排序**：同题合并，按「吐槽次数 + 影响面」排优先级
3. **动手**：挑 1~3 条改进——每条独立改、独立测、独立 git 节点；大改动先向用户确认
4. **回信**：每条意见 `status` 改 `done`（或 `wontfix` + 原因），`reply` 写一句人话说明
5. **记日志**：在 `data/feedback/changelog.md` 顶部追加一条成长记录
6. **释放排班表**：`data/ai-shift.json` 的 holder 置空
7. **汇报**：向用户说清——处理了哪几条、各自怎么改的、跳过了哪几条为什么

## 安全红线

- 改动小步走：一次只动一件事，测试通过才打节点
- 不确定用户意图的意见 → 回信提问，别猜着改
- 涉及删数据、改架构、花钱（API key、新硬件）→ 必须先问用户
- 每次处理完，意见箱里不应再有任何 `"status": "new"` 的意见
