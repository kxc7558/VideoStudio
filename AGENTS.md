# AGENTS.md — AI 通用上岗证

> 任何 AI（Claude Code / Codex / MiMo / zcode / 其他）进入本项目都从本文件开始。
> 目标：无障碍上岗、有序干活。

## 上岗四步

1. 读 `CLAUDE.md`（项目宪法 + 项目记忆，唯一权威）
2. 读你所在层的 `README.md`（末尾「对外接口清单」是跨层契约）
3. 占班：写 `data/ai-shift.json`（holder=你的名字、task=要干的事、started_at=现在）
4. 干活：小步改 → 跑测试 → git 节点 → 干完释放排班表（holder 置空）

## 有序使用（防打架铁律）

- **同一时刻只允许一个 AI 在岗改代码**。排班表显示在岗且 2 小时内有更新 → 等待或只做只读工作
- 在岗者超过 2 小时无更新 → 视为离岗，可接管（commit message 里注明接管）
- 每完成一块立刻 git commit 打节点
- AI 之间交接以各层 README 的「对外接口清单」为准，不靠口头转述

## AI 第一性承诺

- 本项目一切能力都有两个入口：给人看的界面 + 给 AI 用的文件与接口
- 所有 AI 需要的知识都在仓库文件里，不依赖任何特定工具的私有记忆
- 机器可读速查表（怎么跑/怎么测/路径/端口）见 `project.yaml`

## 意见箱（自我迭代原料）

- 用户吐槽落盘 `data/feedback/*.json`，格式见 `api/意见箱说明.md`
- 用户说「处理意见箱」→ 按 `处理意见箱.md` 执行（先占班）
- 每次处理完在 `data/feedback/changelog.md` 记成长日志
